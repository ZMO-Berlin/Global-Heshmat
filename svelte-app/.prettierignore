# Package
node_modules
package-lock.json

# Build
.svelte-kit
build
dist

# Browser test output
playwright-report
test-results

# Hand-edited data files — kept out of Prettier so colleagues can add or
# tweak entries without formatting churn failing CI. Both collections are
# ignored symmetrically (artworks AND residences).
src/lib/data/artworks/[0-9]*.ts
src/lib/data/residences/Residence*.ts
